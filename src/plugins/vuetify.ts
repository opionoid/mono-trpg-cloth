import Vue from "vue";
import Vuetify from "vuetify/lib";

Vue.use(Vuetify);

export default new Vuetify({
  themes: {
    light: {
      primary: "#221B2E",
      seconday: "#837A81",
      accent: "#DCC09B"
    }
  }
});
